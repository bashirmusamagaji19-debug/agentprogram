# Work Log — 2026-08-31 阶段 5（前半）：合成简历端到端验证

> 分支：`feature/chinese-job-pipeline` · 用户暂无真实简历，先用合成简历验证管线。
> **诚实声明**：本轮是管线验证，不满足主计划"真实简历 + 人工确认 Top-5 ≥3 值得投"的验收——真实简历轮待用户提供后补做。

## 一、输入

- 4 份合成简历（`evaluations/synthetic-resumes/`，commit `ba6a54f`）：
  LLM 应用开发（正向）/ CV 算法（方向区分）/ Java 后端（负向对照）/ 数据分析（边界）
- job-radar 最新 jobs.json：3704 条，237 条实习+AI（12 条进管线）
- 主简历跑完整 LLM 栈（qwen 抽取兜底 + 匹配兜底）；其余三份纯规则（零 API）

## 二、跑出来的 3 个新 bug（stories #21–#22 + 修复）

| # | Bug | 修复 commit |
|---|---|---|
| 21 | 腾讯青云计划岗官方 API 拿不到正文 → jd_text 兜底只有 49 字符标题行 → **LLM 幻觉编造整段任职要求还标置信度 1.00**。统计发现 job-radar jd_text 中位数仅 46 字符（标题行，非 JD 摘要），50 字符门槛连浏览器兼容提示都放过 | `f74e14c`：门槛 50→120 + jd_text 同门槛 + extractor 正文 <100 字符禁调 LLM |
| 22 | 官方 API requirements 是编号长句，逗号切分产出句子碎片 → **纯规则匹配在真实中文 JD 上全 0**（CV 简历会 PyTorch、岗位要求 pytorch 也 0 分），此前被 LLM 兜底掩盖 | `dd2a535`：skills 碎片化时降级 JD 文本技能词扫描（canonical 去重），标注集 0.94 零回归 |

修复前 12 页 6 valid（含 1 条幻觉 JD + 垃圾内容混入）；修复后 12 URL → **7 页诚实失败**（腾讯 5 条 API 盲区 + wecruit 2 条 JS 壳）→ 5 valid 全部真实内容零幻觉。

## 三、匹配区分度矩阵（修复后）

| 岗位 | LLM应用 | CV算法 | Java后端 | 数据分析 |
|---|---:|---:|---:|---:|
| 大模型应用实习生（服务智能，美团） | **0.82** | 0.33 | 0.00 | 0.00 |
| 商业分析实习生（大模型应用BP，美团） | 0.68 | 0.33 | 0.00 | 0.00 |
| 大模型算法工程师（实习生，美团） | **0.72** | 0.50 | 0.00 | 0.00 |
| 投资实习生（AI与具身智能，网易） | 0.00 | 0.00 | 0.00 | 0.00 |
| 大模型算法实习生（美团） | **0.72** | 0.43 | 0.00 | 0.00 |

- **方向区分正确**：LLM 应用 > CV 算法（部分重叠合理——同属 AI 技术方向）> Java 后端/数据分析（正确全拒）
- **投资岗全 0 正确**：无技能抽取（网易 hr.163.com 非官方 API 站，抽取质量差，company 抽成了 JD 里的一句话——新站点适配边界，诚实记录不修）
- **LLM 乐观偏差真实复现**：商业分析岗（要 SQL/数据分析）给 LLM 应用简历 0.68——与标注集 #12 结论一致
- 人工判定 Top-5：0.82/0.72/0.72 三个算法/应用岗值得投（3/5 达标线），商业分析方向不符，投资岗 0 分正确

## 四、已知边界（本轮确认，未修）

1. **腾讯青云计划岗不可覆盖**：ByPostId 对这类岗位返回不可用 → jd_text 无信息 → 诚实失败。需要浏览器路径（阶段 4 已裁剪，按需重启）
2. **wecruit.hotjob.cn 类第三方站**：JS 壳 + 兼容提示，无官方 API → 失败分类
3. **网易 hr.163.com**：http 可达但抽取质量差（company 字段抽成句子）——LLM 兜底能补 requirements 但字段质量不稳定
4. **文本扫描模式的覆盖面受词表限制**：是"方向匹配"不是逐条核对，语义边界仍靠 LLM 兜底

## 五、遗留

- **真实简历轮**（主计划阶段 5 验收）：等用户提供简历后重跑 + 人工复核 Top-5
- README"中文岗位真实使用"章节 + release-check（阶段 5 收尾，待真实简历轮后一起做）
