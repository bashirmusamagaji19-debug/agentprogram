# Minimal deps for cloud deployment (Streamlit Community Cloud etc.).
# browser-use is intentionally excluded: the browser path is lazy-loaded in
# browser.py and not needed for Demo / aggregator-JSON / HTTP fetching or LLM
# extraction & matching. Keep this file ASCII-only for locale-safe pip parsing.
streamlit>=1.41,<2
langgraph>=0.2.60
langgraph-checkpoint-sqlite>=3.1,<4
pydantic>=2.7
python-dotenv>=1.0
